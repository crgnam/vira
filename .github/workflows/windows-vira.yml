name: Windows Vira Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  windows-library:
    runs-on: windows-2022 # Updated August 2025
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Set up build environment
      uses: ./.github/actions/setup-build
      with:
        platform: windows
    
    - name: Configure project
      uses: ./.github/actions/configure-cmake
      with:
        platform: windows
    
    - name: Build project
      uses: ./.github/actions/build-cmake
      with:
        platform: windows